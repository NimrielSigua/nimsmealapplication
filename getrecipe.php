<?php
// include 'api/connection.php';
   
// $query = "SELECT recipe_id, recipe_name FROM recipetbl"; // Retrieve both recipe_id and recipe_name
// $stmt = $conn->prepare($query);
// $stmt->execute();

// $recipes = [];
// while ($row = $stmt->fetch(PDO::FETCH_ASSOC)) {
//     $recipes[] = $row; // Store both recipe_id and recipe_name in the array
// }

// echo json_encode($recipes); // Encode the array containing recipe_id and recipe_name

// $conn = null; // Close the connection
?>

